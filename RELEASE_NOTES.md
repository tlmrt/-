# 开源日历 v0.1.2

## 🛠 修复：开机自启「勾选后又变回未勾选」

**现象**：勾上「开机自动启动」，关掉设置再打开，勾选框又空了 —— 看起来像没保存。

**真相**：这一项其实**早就写进系统启动项了**（注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 下的 `cn.evestudio.calendar` 一直在，开机确实会自启），出问题的是"读回"。Electron 在 Windows 上判断启动项时，会拿「可执行文件路径 + 命令行参数」去比对注册表内容；我们的启动项带了 `--hidden`（后台静默启动用），读取时却没带这个参数，于是永远匹配不上、界面永远显示未勾选。

**修复**
- 写入与读取共用同一份启动命令行（exe + `--hidden`），读取按同一份参数比对（实测：旧读法 `false`，新读法 `true`）
- 增加注册表兜底核对：若 Electron 仍判定未启用，就直接读注册表 `Run` 项原文比对，兼容 Electron 版本差异
- 勾选框**永远反映系统里的真实状态**：写入后立即回读，若确实没写进去（如被安全软件拦截）会纠正勾选框并告诉你手动添加的方法
- 启动时打印一行开机自启状态日志，方便日后排查

## ➕ 设置面板新增「保存设置」按钮

- 面板底部右侧新增「保存设置」（在「完成」左边）：点击后把基础设置与开机自启一次性写入本机，并在左侧回显「设置已保存到本机」；若有未生效的项，会直接写明原因
- 点「完成」关闭设置前也会静默保存一次，避免改了却没落盘

## 🧪 其它

- 新增 14 项开机自启单测（`test/autostart.test.js`），全套 **265 项**通过

## 📦 下载与安装

| 文件 | 说明 | SHA256 |
|---|---|---|
| `开源日历 Setup 0.1.2.exe` | 安装版（推荐）：覆盖升级，数据保留 | `765A6A745B9D13A2772F9C4E4F382E8B87450E0B829133807C05CF52F26C171F` |
| `开源日历 便携版 0.1.2.exe` | 免安装便携版，双击即用 | `AFEC1A6E298BA278D375A71FDB2DDAEBD56EA45F4DD0DEF212876426F9723A8B` |

- 系统要求：Windows 10 / 11（x64）
- 数据保存在本机 `%APPDATA%\evestudio-calendar\`，覆盖安装不会丢

---

# 开源日历 v0.1.1

## 🛠 修复：装了加速器 / 代理的电脑上「检查更新」必然失败

**现象**：点「设置 → 应用更新 → 检查更新」永远失败，提示类似
`unable to verify the first certificate` 或「检查更新失败」。

**原因**：应用原来用 Node 自带的网络实现访问 GitHub，而它**只认 Node 内置的证书列表、不读 Windows 证书库、也不走系统代理**。本机若装了 Steam++ / Watt Toolkit 这类 GitHub 加速器（或 Clash 等代理），它们会把 `github.com`、`api.github.com` 等域名解析到 `127.0.0.1` 由本机做中间人转发，其根证书装在 Windows 证书库里 —— 浏览器认，Node 不认，于是检查更新和下载更新全部失败。

**修复**
- 主进程的联网请求（检查更新 / 下载安装包 / 节假日数据）统一改走 **Electron 的 Chromium 网络栈**（读 Windows 证书库 + 走系统代理），失败再自动回退 Node 直连；两条通道的错误会合并上报，方便定位。开着加速器现在也能正常检查更新。
- 失败提示换成人话，并按原因分类：证书不被信任 / 断网或代理没开 / 域名解析失败 / 仓库或 Release 不存在 / API 频率限制 —— 每种都给出下一步该怎么办，并附带原始错误。
- 设置里的「打开发布页」按钮改为**常显**：即使检查失败，也始终有一条「用浏览器手动下载安装包」的退路（检查失败时该按钮打开仓库 Releases 页）。
- 节假日在线更新：单一数据源失败后自动换镜像（jsdelivr cdn / fastly / gcore / raw），不再因为某个源抽风就整体更新失败；实测 `raw.githubusercontent.com` 在本机会 502，而三个 jsdelivr 节点均正常。
- 新增 17 项单测（`test/updater.test.js`、`test/holidays.test.js`），全套 **251 项**通过。

**如果你正卡在这个问题上**：由于出问题的正是"检查更新"本身，自动更新拿不到这个修复，需要手动装一次本版本；装好后（v0.1.1 起）即可恢复应用内一键检查 / 下载更新。

## 📦 下载与安装

| 文件 | 说明 | SHA256 |
|---|---|---|
| `开源日历 Setup 0.1.1.exe` | 安装版（推荐）：覆盖升级，数据保留 | `8F43596C452CC0CF1A0C5924A790F19490E471D6297B6C32D119DF03A4ECC693` |
| `开源日历 便携版 0.1.1.exe` | 免安装便携版，双击即用 | `20D1FCB674FA5C6AF138C9390A4F83426E05C6B8F6B56A8C7014A2902E6CD96E` |

- 系统要求：Windows 10 / 11（x64）
- 首次运行若出现 SmartScreen 提示，选择"更多信息 → 仍要运行"（未做代码签名）
- 数据全部保存在本机 `%APPDATA%\evestudio-calendar\`，不上传任何数据

## 🔄 更新

应用内 **设置 → 应用更新** 可检查更新（仓库地址已锁定，指向本项目）。发布新版本时把安装包（文件名建议含 `Setup`）上传到 Release，应用即可自动发现并一键下载更新。

## ❤ 赞助

完全免费开源（MIT）。如果它帮到了你，欢迎在应用内点标题栏的 ❤ 赞助支持作者。

## 📄 许可

MIT License，见仓库中的 LICENSE。

---

# 开源日历 v0.1.0（历史版本）

首个公开版本 🎉 一个完全本地、开源的 Windows 桌面日历 + 任务到点提醒工具，并内置与 **MAA** 的深度联动。

## ✨ 主要功能

**日历与任务**
- 月视图日历：单击选日、双击新建任务；日期格内直接显示当天任务（时间 + 标题，按优先级着色）
- 任务支持备注、标签、优先级、重复（每天 / 工作日 / 每周 / 每月，月末自动收敛）
- **多条提醒**：每条提醒可选动作 —— 「弹通知提醒」或「🎮 启动 MAA」
- Windows 系统通知 + 失败兜底置顶小窗；可自定义提醒语音（音频文件）与系统 TTS 朗读
- 托盘常驻、开机自启（后台静默运行、单实例防重复提醒）

**外观**
- 强调色 / 背景色双通道自定义配色，面板透明度、悬停虚化背景
- 背景支持单张图片、**图片幻灯片**（最多 100 张、间隔可调、随机一轮不重复）、**视频**背景

**特色**
- ⏳ 时间段液体倒计时：时间段进行中，日历格内液体随剩余时间下降
- 🪟 桌面小组件：把日期格子拖到桌面，独立小窗显示当天任务与液体
- 🗓 节日与农历：6 个国家/地区节日可多选、可逐个开关，显示农历日期
- 🎉 法定休息日：可在线更新放假安排（含调休），格子标注「休」「班」
- 🧩 插件口：把 `.js` 丢进插件目录即可扩展（含教程与示例）
- 🔌 本地 HTTP 接口（127.0.0.1:8765，令牌鉴权）+ webhook 事件推送，方便与其它软件联动

**MAA 联动**
- 从日历直接打开 MAA「一键长草」式的任务配置面板：逐任务勾选与改参数，保存写回 MAA 配置
- 关卡选择器：读 MAA 自带关卡数据，支持搜索 / 自动修正（`ce6` → `CE-6`）/ 分组搜索（含剿灭）/ 当期活动置顶 / 开放状态提示
- 按日期绑定不同的 MAA 配置；全局设置可**每天定时自动启动 MAA**
- 自动搜索电脑上的 MAA 并一键设置路径

## 📦 该版本下载（历史）

| 文件 | 说明 | SHA256 |
|---|---|---|
| `开源日历 Setup 0.1.0.exe` | 安装版 | `73472BF4D6BB1CB5F12DDFCAF698F21F759FAF860ECF7D426BC2DE53E4CB2B7C` |
| `开源日历 便携版 0.1.0.exe` | 免安装便携版 | `E33F81AE1F4AA878666F36D0FAB561975D848EC6A3511CC9CEB948DD4F4B851D` |
