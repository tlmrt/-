# 开源日历 v0.1.13

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🎮 MAA 监视窗口现在能看到"进行到哪一步"了

以前小组件只知道 MAA 在不在跑；现在它会显示**当前正在执行的任务**，数据来自 MAA 自己的运行日志。

**MAA 运行中**：
- 大字显示 **`正在：理智作战` / `正在：基建换班`** —— 就是你配置里的任务名
- 下一行显示 **`刚完成：访问好友`**（连子步骤都能看到）或 `第 3 步`
- **超过 5 分钟没有新进展**时会变红提示 `已 X 分钟没有新进展`，一眼看出是不是卡住（在等模拟器 / 卡关卡）
- 跑完后显示 **`本次任务已全部完成` + `用时 0h 23m 13s · 理智 09-13 04:36 回满`**

**MAA 没在跑时**也不空着：
- `今日计划：08:00 · 日常配置`
- `上次运行：已全部完成（用时 0h 23m 13s）· 约 5 小时前`

**其它**：刷新间隔 15 秒 → **5 秒**（更跟手）；窗口高度略增以容纳新信息；本地接口 `GET /api/maa/status` 也返回 `progress`（`line1/line2/tone/currentTask/elapsedText`），方便外部软件联动。

**实现**：MAA 会把每一步写进自己目录下的 `debug/gui.log`（`开始任务: 领取奖励` / `完成任务: 访问好友` / `任务已全部完成！(用时 …)`）。新模块 `src/maalog.js` 只读日志**尾部 200KB** 并按 `size+mtime` 缓存，每秒刷新也几乎不耗资源；27 项单测用真实日志片段覆盖了进行中、子步骤、全部完成、卡住、截断等情形。

---

# 开源日历 v0.1.12

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🌊 周视图的时间段会"流"了

原来只是一块半透明色块，现在把月视图那套液体语言搬了过来：

- **液面随时间下降**：未开始是满的，进行中按剩余比例慢慢流干，结束后见底（每秒推进，1 秒线性过渡，不会一跳一跳）
- **水面波浪**：液面顶部有持续滚动的波浪纹理，和日程格子里的液体同一种视觉
- **进行中会呼吸发光**：正在走的那个时间段有脉冲式外发光，一眼就能看出"现在是它"
- **信息更全**：块内显示标题 + `起-止 · 剩 1小时29分`，颜色取自该时间段自己的液体色
- **可交互**：鼠标移上去有高亮反馈，**点一下直接打开这个时间段的编辑弹窗**（以前色块是点不动的）

实测：建一个"现在 → 90 分钟后"的时间段，切到周视图 → 色块 `active`、液面 98.9%、显示"13:37-15:07 · 剩 1小时29分"，点击正确打开编辑且类型/结束时间回填无误。

---

# 开源日历 v0.1.11

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🔀 时间段并入「添加任务」，并与周视图联动

**之前**：时间段是独立的一套东西 —— 日期面板上单独的 ⏳ 按钮 + 独立弹窗 + 独立的 `segments.json`，跟任务两不相干。

**现在**：时间段就是**任务的一种类型**。

- 日期面板上的 **⏳ 入口已删除**，原「时间段」弹窗一并移除
- 新建/编辑任务时多了 **「类型」** 选择：
  - 普通任务（到点提醒）
  - **时间段（液体倒计时）** → 选中后出现「结束时间」与「液体颜色」，默认时长 2 小时
- 时间段任务享有的能力跟普通任务完全一样：多条提醒（含到点启动 MAA）、标签、优先级、备注、一句话快速填写
- **周视图联动**：时间段在周视图里显示为**跨时间的色块**（高度按起止时间），不再是孤立的点；日历格里的液体倒计时照旧
- 数据只有一份：`segments` 变成**从任务派生的内存视图**，删任务 = 时间段一起消失
- **老数据自动迁移**：首次启动会把原来的 `segments.json` 转成时间段任务（旧文件改名 `segments.json.migrated.bak` 留底），你之前设的时间段不会丢

**实测**（端到端）：新建时间段 14:00–16:30 → 保存后任务 `segment:true / endTime:16:30` → 自动派生出对应时间段 → **周视图出现 1 个时间块（且不重复出现任务块）** → 月视图格子里出现液体线 → 删除任务后时间段同步消失。

---

# 开源日历 v0.1.10

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🐛 修复：切到周视图后月历仍占着大半个屏幕

**原因**：切换视图用的是 HTML 的 `hidden` 属性，但浏览器对 `[hidden]` 的默认样式**优先级很低** —— 只要元素自身有类设置了 `display`，类选择器就会把它盖掉。月网格有 `.cal-grid { display: grid; flex: 1 }`、表头有 `.week-head { display: grid }`，于是 `hidden` 形同虚设，月历照旧占位，周视图被挤到下面。

**修复**：在样式表里加一条全局规则，让 `hidden` 永远真的隐藏：

```css
[hidden] { display: none !important; }
```

这是**系统性问题**，不止周视图：其它用 `hidden` 控制显隐的 flex 元素（例如背景视频声音行、幻灯片配置区）以前也可能"藏不掉"，这一条一起治了。

**实测**（读取切换后的真实布局）：
- 月模式：`calGrid display=grid 高=664` · `weekView display=none 高=0`
- 周模式：`calGrid display=none 高=0` · `calHead display=none` · `weekView 高=698`（占满）
- 切回月：正常复原

---

# 开源日历 v0.1.9

> 仅本机迭代，按要求**暂未上传 GitHub**。

这一版是**代码与体积优化**。

## ⚡ 体积：安装包 98.33 MB → 92.17 MB
- 新增打包钩子 `build/afterPack.js`，裁掉用不到的 Chromium 组件：
  - `dxcompiler.dll`（DirectX Shader Compiler，**24.6 MB**）—— 只有 WebGPU 用得上，本应用不用
  - `resources/app-update.yml` —— 本项目用内置的 GitHub 更新器，不需要它
- **明确保留**（都有用途，不拿兼容性换体积）：`ffmpeg.dll`（视频/音频解码）、`vk_swiftshader.dll`（无显卡 / 远程桌面的软件渲染后备）、`d3dcompiler_47.dll`（老显卡）、`LICENSES.chromium.html`（合规）
- 实测：裁掉后应用启动、渲染、日志**无任何 GPU / 崩溃错误**后，才写进构建流程
- 解包体积 **321.8 MB → 297.3 MB**

> 还能不能更小？剩下的大头是 Electron 运行时本身（主程序 234.7 MB + Chromium 许可 19.5 MB + 资源包 22 MB），属于运行必需；再往下只能牺牲老显卡 / 无显卡环境的兼容性，我选择不做。

## 🧹 代码优化
- 命令面板的日期解析改为**复用 `src/nlp.js`**（经 IPC，加了输入竞态保护），删掉渲染层重复实现的那一份日期规则 —— 代码更少，能力反而更强：现在能识别「下周三」「月底」「10月1日」这类写法
- 农历库（约 425 KB）改为**按需加载**，不再拖慢启动
- 插件脚本加载加 5 秒超时并统一收集错误

## 🧪 测试
- **572 项 / 13 个套件**全部通过；装机后实测启动无错、接口正常、开机自启与备份均照常工作

---

# 开源日历 v0.1.8

> 仅本机迭代，按要求**暂未上传 GitHub**。

清单上剩下的 6 项一起完成。

## 📅 周视图 / 日程时间轴
- 标题栏新增 **月 / 周** 切换；周视图按小时排布一周 7 天
- 任务块按时间定位（优先级配色）、时间段显示为半透明色块
- **点空白格**直接建任务（自动预填该日与该小时），**点任务块**打开编辑
- 上一周 / 下一周 用 ‹ ›，点「今天」回到本周；改用「每周从哪天开始」设置自动适配

## 📣 每日早报
- 设定时间发一条：今天几个任务、最近一条是几点、是否休息日/调休、有没有时间段
- 开关与时间在 设置 → 提醒与启动

## 🌙 免打扰时段
- 时段内到点的提醒**不弹通知、不响声音**，只记一笔；**时段结束后发一条汇总**告诉你夜里被静音了哪些
- 支持跨夜（如 23:00 → 次日 07:00）；任务里的「启动 MAA」与 webhook 推送不受影响

## 🩺 内置诊断面板
- 设置 → 数据备份 → 「诊断信息…」：一屏看全程序版本与运行时、数据目录、本地接口、更新检查结果、MAA 路径与计划、备份与插件、各数据文件大小、**最近 40 条 warn/error 日志**
- 支持一键「复制全部」，排查问题直接粘贴

## 🧩 插件超时与隔离
- 插件脚本加载加 **5 秒超时**，坏插件不再拖住启动
- 插件加载失败与运行期抛错统一收集（诊断面板会显示），单个插件出错不影响应用与其它插件

## ⚡ 农历数据懒加载
- lunar-javascript（约 425KB）改为**首次用到农历时才加载**，缩短启动时间

## 🧪 测试
- 新增免打扰时段单测 23 项，合计 **572 项 / 13 个套件**全通过
- 真实渲染环境自检：周视图 7 列 × 24 小时、点空白建任务预填正确、诊断信息各段齐全、免打扰与早报控件就位

---

# 开源日历 v0.1.7

> 仅本机迭代，按要求**暂未上传 GitHub**。

这一版把你点名的 7 项一起做了。

## 🗄 数据自动备份 + 一键恢复
- 每天首次启动自动备份一次，把任务 / 设置 / 时间段 / 小组件 / 贴纸图打包成**一份 JSON** 存到 `backups/`，只保留最近 7 份
- 设置新增「🗄 数据备份」分类：立即备份 / 打开备份目录 / 打开数据目录 / 选一份**恢复**
- **恢复前会自动把当前数据再备份一份**，所以恢复错了也能回头；恢复后界面立刻刷新，不用重启
- 备份文件名与内容都做过安全过滤（防路径穿越），坏文件不会影响恢复

## ⌘ 命令面板与快捷键（Ctrl+K）
- `Ctrl+K`（或 `Ctrl+P`）呼出命令面板：搜任务、跳日期、新建任务、执行命令
- 支持直接输入日期跳转：`明天`、`周一`、`10-01`、`10月1日`
- 快捷键：`Ctrl+N` 新建任务、`Ctrl+,` 打开设置、`T` 回到今天、`←/→` 切换月份、`Esc` 关闭弹窗

## ⏰ 提醒卡上的「稍后 10 分钟」
- 点通知会弹出提醒卡：**稍后 10 分钟 / 稍后 1 小时 / 打开日历 / 关闭**
- 推后的提醒到点会重新弹，并标注"第 N 次稍后"

## ✍️ 一句话建任务
- 新建任务弹窗顶部新增「一句话快速填写」，**回车自动填好日期、时间、重复、提醒**
- 例：`明天下午3点 开会 提前10分钟`、`每周一 9:00 站会`、`10月1日 09:00 出行 提前1天`、`后天晚上8点半 健身`
- 支持中文数字（`晚上八点半`）、全角冒号、`提前半小时`、`每周三`、`月底`、月末收敛等

## 🎮 MAA 跑完也发通知
- 以前只有"已启动"，现在 MAA 退出后会通知：`MAA 已结束 · 任务「xx」· 运行约 32 分钟`
- 手动点「停止 MAA」不会误报；同时推送 `maa.finished` webhook

## 📆 每周计划表
- MAA 全局设置里新增 7 行表格：**周日~周六各自设定时间与配置**，未勾选的星期沿用「每天」设置
- 适合周末自动换剿灭 / 活动本配置；当天计划会显示在 MAA 监视小组件上

## 🖥 MAA 状态桌面小组件
- 设置 → 外部联动 里可「＋ 放一个 MAA 监视到桌面」：显示运行状态与时长、今日计划、上次自动启动时间
- 卡片上直接**启动 / 停止 MAA**，配色与日期小组件一样可自定义（🎨）

## 🧪 测试
- 测试规模 **265 → 549 项**（新增备份 112、一句话解析 141、稍后提醒 17、每周计划 14），12 个套件全通过
- 另用真实渲染环境自检：命令面板搜索与跳转、一句话解析、备份创建与列表、每周计划表 7 行、MAA 小组件创建与数据、快捷键绑定，全部符合预期

---

# 开源日历 v0.1.6

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🐛 修复：MAA 全局设置里的输入框与下拉框"点不动 / 选不了"

**根因（我自己的代码写错）**：那个弹窗每次改动设置后都会整块重渲染一遍 —— 重新写回时间输入框的值、并把下拉框的选项 `innerHTML = ''` 清空重建。

于是：
- **时间框**：Chromium 的时间控件每调整一下就触发 `change`，我们立刻把值写回旧的 → 看着就是"点了没反应、不受控"
- **下拉框**：每选一下就重建整个下拉 → 下拉被强行关闭、选中被重置 → "选不了"

**修复**
- 把"完整渲染"与"刷新状态文字"拆开：**改动设置后只更新底部状态行**，绝不再碰输入控件
- 打开弹窗时才做完整渲染（此时覆盖控件、重建选项是安全的）
- 顺带收紧了上一版加的"输入框聚焦兜底"：**只处理文本类输入框**，跳过 `select` 与 `date/time/color/file/range` 等原生控件（这些控件点击时会弹自己的 UI，强行 focus 会打断它们），并且改成延后 80ms、确认仍未聚焦才补

**实测**：改时间后值稳定保持、下拉 DOM 未被重建（节点引用不变）；改下拉后选中保持、选项不被重建。

---

# 开源日历 v0.1.5

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🎨 小组件配色更完整（修「只有上面一部分变色」）

你反馈「小组件颜色只改了上面一部分」—— 查下来是两种情况叠加：

1. **强调色**本身只管局部（顶部色条 / 日期 / 时间 / 圆点），只拖动它自然看着像"上面变了"；
2. 列表区过去是完全透明的，配色变化在那里**看不出来**，整卡显得"上面有色、下面一片白"。

这次的改进：

- **列表区加了同色系淡淡底色**（4% 叠加，玻璃配色下依然通透），整卡配色现在一眼可见
- **任务行加了状态圆点**：高优先级红点、普通跟随强调色、低优先级灰点，层次更清楚
- **列表滚动条**也跟随配色
- 配色面板顶部显示**当前配色名**（墨黑 / 樱花 / … / 自定义）
- 面板里明确标注每个颜色的作用范围：
  `背景` → 卡片与列表底；`文字` → 任务文本与分隔线；`强调` → 顶部色条 · 日期 · 时间 · 圆点

**实测**（读取真实渲染后的计算样式）：选墨黑后卡片、标题栏、列表底、底栏、分隔线全部变深；点「樱花」整卡换色；只调强调色时确实只有色条与日期变 —— 与设计一致，现在面板里也写清楚了。

---

# 开源日历 v0.1.4

> 仅本机迭代，按要求**暂未上传 GitHub**。

## 🎨 桌面小组件可以自定义配色了

- 从日历拖出的小组件不再是清一色白底：标题栏新增 **🎨 按钮**，点开就是配色面板
- **8 套预设**：经典白 / 玻璃（半透明，配桌面背景很好看）/ 墨黑 / 深蓝 / 青瓷 / 抹茶 / 樱花 / 琥珀
- 想自由搭配就用三个取色器：**背景色 / 文字色 / 强调色**（强调色留空则跟随日历主题色）
- 配色**按小组件单独保存**，每个小组件可以不一样，重启后保持
- 顺带美化：顶部加了强调色条；卡片背景、分隔虚线、按钮、空状态文字全部跟随配色，深色主题下也能看清

## 🗂 设置面板分类，不再臃肿

- 设置改成「**左侧分类 + 右侧内容**」两栏：
  🎨 外观 · 📅 日历 · 🔔 提醒与启动 · 🔌 联动与 MAA · 🧩 更新与插件
- 每类只显示相关设置项（3 / 3 / 3 / 1 / 2 项），切换即时生效，并记住你上次停留的分类
- 窗口很窄时自动退化成顶部横向分类，不会挤成一团

## 🧪 测试

- 265 项单测全部通过；另用真实渲染环境自检：分类切换只显示对应字段、小组件配色写入与读回一致

---

# 开源日历 v0.1.3

> 本次仅本机迭代，按要求**暂未上传 GitHub**。

## 🔊 背景视频可以出声音了

- 设置 → 日历背景：选了视频后会出现「播放视频声音」开关 + 音量滑块（**默认仍然静音**，不会突然出声）
- 开启后重启应用也能直接带声音自动播放（补上了 Chromium 的自动播放策略开关）

## 🎨 设置面板更清爽：长说明收进 ⓘ

- 设置里十几段说明文字全部收进选项旁边的圆圈感叹号 ⓘ，鼠标移上去（或键盘 Tab 聚焦）才展开，不再占满版面
- ⓘ 用单例浮层实现：不会被弹窗滚动区裁切，DOM 重绘后也不会失效

## 🐛 修复：部分电脑上「添加任务」后点输入框没反应

- 打开新建 / 编辑任务弹窗时自动把光标放进标题框，不点也能直接打字
- 增加输入框聚焦兜底：少数环境（远程桌面、安全软件注入、输入法冲突）下点击输入框不聚焦，现在会主动补一次聚焦
- 去掉弹窗遮罩的 `backdrop-filter` 模糊（部分显卡 / 远程桌面下会造成点击命中异常），改为纯半透明遮罩
- 弹窗改为留出边距、窗口过矮时可滚动：避免小屏或高缩放下输入框跑出可视区点不到

## ⚡ 体积优化

- Electron 语言包只保留中文与英文：**55 个 / 48.3 MB → 2 个 / 1.1 MB**
- 打包改用最大压缩，并排除依赖里的测试 / 文档目录
- 效果：安装包 **106.6 MB → 98.3 MB**，便携版 106.4 → 98.1 MB，解包后 368.9 MB → 321.8 MB
- 说明：剩下的大头是 Electron 运行时本身（主程序 234.7 MB、dxcompiler 24.6 MB、Chromium 许可文件 19.5 MB），属于运行必需，除非换技术栈否则压不下去

## 🧪 测试

- 265 项单测全部通过

---

# 开源日历 v0.1.2

## 🛠 修复：开机自启「勾选后又变回未勾选」

**现象**：勾上「开机自动启动」，关掉设置再打开，勾选框又空了 —— 看起来像没保存。

**真相**：这一项其实**早就写进系统启动项了**（注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 下的 `cn.evestudio.calendar` 一直在，开机确实会自启），出问题的是"读回"。Electron 在 Windows 上判断启动项时，会拿「可执行文件路径 + 命令行参数」去比对注册表内容；我们的启动项带了 `--hidden`（后台静默启动用），读取时却没带这个参数，于是永远匹配不上、界面永远显示未勾选。

**修复**
- 写入与读取共用同一份启动命令行（exe + `--hidden`），读取按同一份参数比对（实测：旧读法 `false`，新读法 `true`）
- 增加注册表兜底核对：若 Electron 仍判定未启用，就直接读注册表 `Run` 项原文比对，兼容 Electron 版本差异
- 勾选框**永远反映系统里的真实状态**：写入后立即回读，若确实没写进去（如被安全软件拦截）会纠正勾选框并告诉你手动添加的方法
- 启动时打印一行开机自启状态日志，方便日后排查

## ➕ 设置面板新增「保存设置」按钮

- 面板底部右侧新增「保存设置」（在「完成」左边）：点击后把基础设置与开机自启一次性写入本机，并在左侧回显「设置已保存到本机」；若有未生效的项，会直接写明原因
- 点「完成」关闭设置前也会静默保存一次，避免改了却没落盘

## 🧪 其它

- 新增 14 项开机自启单测（`test/autostart.test.js`），全套 **265 项**通过

## 📦 下载与安装

| 文件 | 说明 | SHA256 |
|---|---|---|
| `开源日历 Setup 0.1.2.exe` | 安装版（推荐）：覆盖升级，数据保留 | `765A6A745B9D13A2772F9C4E4F382E8B87450E0B829133807C05CF52F26C171F` |
| `开源日历 便携版 0.1.2.exe` | 免安装便携版，双击即用 | `AFEC1A6E298BA278D375A71FDB2DDAEBD56EA45F4DD0DEF212876426F9723A8B` |

- 系统要求：Windows 10 / 11（x64）
- 数据保存在本机 `%APPDATA%\evestudio-calendar\`，覆盖安装不会丢

---

# 开源日历 v0.1.1

## 🛠 修复：装了加速器 / 代理的电脑上「检查更新」必然失败

**现象**：点「设置 → 应用更新 → 检查更新」永远失败，提示类似
`unable to verify the first certificate` 或「检查更新失败」。

**原因**：应用原来用 Node 自带的网络实现访问 GitHub，而它**只认 Node 内置的证书列表、不读 Windows 证书库、也不走系统代理**。本机若装了 Steam++ / Watt Toolkit 这类 GitHub 加速器（或 Clash 等代理），它们会把 `github.com`、`api.github.com` 等域名解析到 `127.0.0.1` 由本机做中间人转发，其根证书装在 Windows 证书库里 —— 浏览器认，Node 不认，于是检查更新和下载更新全部失败。

**修复**
- 主进程的联网请求（检查更新 / 下载安装包 / 节假日数据）统一改走 **Electron 的 Chromium 网络栈**（读 Windows 证书库 + 走系统代理），失败再自动回退 Node 直连；两条通道的错误会合并上报，方便定位。开着加速器现在也能正常检查更新。
- 失败提示换成人话，并按原因分类：证书不被信任 / 断网或代理没开 / 域名解析失败 / 仓库或 Release 不存在 / API 频率限制 —— 每种都给出下一步该怎么办，并附带原始错误。
- 设置里的「打开发布页」按钮改为**常显**：即使检查失败，也始终有一条「用浏览器手动下载安装包」的退路（检查失败时该按钮打开仓库 Releases 页）。
- 节假日在线更新：单一数据源失败后自动换镜像（jsdelivr cdn / fastly / gcore / raw），不再因为某个源抽风就整体更新失败；实测 `raw.githubusercontent.com` 在本机会 502，而三个 jsdelivr 节点均正常。
- 新增 17 项单测（`test/updater.test.js`、`test/holidays.test.js`），全套 **251 项**通过。

**如果你正卡在这个问题上**：由于出问题的正是"检查更新"本身，自动更新拿不到这个修复，需要手动装一次本版本；装好后（v0.1.1 起）即可恢复应用内一键检查 / 下载更新。

## 📦 下载与安装

| 文件 | 说明 | SHA256 |
|---|---|---|
| `开源日历 Setup 0.1.1.exe` | 安装版（推荐）：覆盖升级，数据保留 | `8F43596C452CC0CF1A0C5924A790F19490E471D6297B6C32D119DF03A4ECC693` |
| `开源日历 便携版 0.1.1.exe` | 免安装便携版，双击即用 | `20D1FCB674FA5C6AF138C9390A4F83426E05C6B8F6B56A8C7014A2902E6CD96E` |

- 系统要求：Windows 10 / 11（x64）
- 首次运行若出现 SmartScreen 提示，选择"更多信息 → 仍要运行"（未做代码签名）
- 数据全部保存在本机 `%APPDATA%\evestudio-calendar\`，不上传任何数据

## 🔄 更新

应用内 **设置 → 应用更新** 可检查更新（仓库地址已锁定，指向本项目）。发布新版本时把安装包（文件名建议含 `Setup`）上传到 Release，应用即可自动发现并一键下载更新。

## ❤ 赞助

完全免费开源（MIT）。如果它帮到了你，欢迎在应用内点标题栏的 ❤ 赞助支持作者。

## 📄 许可

MIT License，见仓库中的 LICENSE。

---

# 开源日历 v0.1.0（历史版本）

首个公开版本 🎉 一个完全本地、开源的 Windows 桌面日历 + 任务到点提醒工具，并内置与 **MAA** 的深度联动。

## ✨ 主要功能

**日历与任务**
- 月视图日历：单击选日、双击新建任务；日期格内直接显示当天任务（时间 + 标题，按优先级着色）
- 任务支持备注、标签、优先级、重复（每天 / 工作日 / 每周 / 每月，月末自动收敛）
- **多条提醒**：每条提醒可选动作 —— 「弹通知提醒」或「🎮 启动 MAA」
- Windows 系统通知 + 失败兜底置顶小窗；可自定义提醒语音（音频文件）与系统 TTS 朗读
- 托盘常驻、开机自启（后台静默运行、单实例防重复提醒）

**外观**
- 强调色 / 背景色双通道自定义配色，面板透明度、悬停虚化背景
- 背景支持单张图片、**图片幻灯片**（最多 100 张、间隔可调、随机一轮不重复）、**视频**背景

**特色**
- ⏳ 时间段液体倒计时：时间段进行中，日历格内液体随剩余时间下降
- 🪟 桌面小组件：把日期格子拖到桌面，独立小窗显示当天任务与液体
- 🗓 节日与农历：6 个国家/地区节日可多选、可逐个开关，显示农历日期
- 🎉 法定休息日：可在线更新放假安排（含调休），格子标注「休」「班」
- 🧩 插件口：把 `.js` 丢进插件目录即可扩展（含教程与示例）
- 🔌 本地 HTTP 接口（127.0.0.1:8765，令牌鉴权）+ webhook 事件推送，方便与其它软件联动

**MAA 联动**
- 从日历直接打开 MAA「一键长草」式的任务配置面板：逐任务勾选与改参数，保存写回 MAA 配置
- 关卡选择器：读 MAA 自带关卡数据，支持搜索 / 自动修正（`ce6` → `CE-6`）/ 分组搜索（含剿灭）/ 当期活动置顶 / 开放状态提示
- 按日期绑定不同的 MAA 配置；全局设置可**每天定时自动启动 MAA**
- 自动搜索电脑上的 MAA 并一键设置路径

## 📦 该版本下载（历史）

| 文件 | 说明 | SHA256 |
|---|---|---|
| `开源日历 Setup 0.1.0.exe` | 安装版 | `73472BF4D6BB1CB5F12DDFCAF698F21F759FAF860ECF7D426BC2DE53E4CB2B7C` |
| `开源日历 便携版 0.1.0.exe` | 免安装便携版 | `E33F81AE1F4AA878666F36D0FAB561975D848EC6A3511CC9CEB948DD4F4B851D` |
